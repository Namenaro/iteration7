from utils import *
from deviceA import *
from u import *

def make_u_from_A(json_data, A, u):
    lead="i"
    result = []
    for patient_id in json_data.keys():
        ecg_json = json_data[patient_id]
        signal
        dxs, patches = u.do(json_data, )
    return result